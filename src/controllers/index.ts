import idx from './idx.ctr.ts';
import auth from './auth.ctr.ts';
import reg from './reg.ctr.ts';
import acc from './acc.ctr.ts';
import rst from './rst.ctr.ts';

const ctr = {
  idx: idx,
  auth: auth,
  reg: reg,
  acc: acc,
  res: rst,
};

export default ctr;
